name: Regenerate spatial ore Continuity assets

on:
  push:
    branches:
      - fix/ore-spatial-continuity-20260901
    paths:
      - scripts/generate_ore_continuity_tilesets.py
      - scripts/validate_ore_continuity_tilesets.py
      - .github/workflows/qa-ore-spatial-regeneration.yml

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out QA branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Pillow
        run: python -m pip install --disable-pip-version-check pillow

      - name: Generate and validate spatial ore assets
        run: |
          python3 scripts/generate_ore_continuity_tilesets.py
          python3 scripts/validate_ore_continuity_tilesets.py

      - name: Commit generated resources
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/main/resources/assets/geostrata/optifine/ctm/ore \
            src/main/resources/assets/geostrata/textures/optifine/ctm/ore \
            src/main/resources/data/geostrata/materials/ore_ctm_manifest.json \
            docs/images/ore-ctm-tileset-preview.png
          if git diff --cached --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git commit -m "Regenerate multi-block ore Continuity textures"
          git push origin HEAD:${GITHUB_REF_NAME}
